import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Jason A. Wise",
  description: "...",
};

export default async function Page() {
  return (
    <div>
      <h1>Welcome!</h1>
    </div>
  );
}
